# Master originals — kept locally as backup, never pushed to the repo/site.
/Events/
/Exhibitions/
/Pavilions/
/B2B/

# Web images now served from Cloudflare R2 (media.megapressevents.com), not the repo.
# Local copies stay on disk; they just aren't tracked or deployed anymore.
/thumbs/
/watermarked/

# Build artifact — the site is generated in place into index.html
index.built.html
